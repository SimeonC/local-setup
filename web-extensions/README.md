# Important Notes

* When writing scripts do not use the `'` (single quote) character as some extensions have bugs where restarting the browser will duplicate them in the file for some reason. It's a simple fix to just either use backticks or double quotes.