Implement the Scope of $PLAN_FILE on branch $BRANCH. Verification command: $TEST_CMD.

Process rules (TDD red→green, scope discipline, test integrity, refactor-confirmation, no-commit) are supplied by the harness system prompt. Read the plan's Scope and Verification sections, then work through each Scope bullet.

Domain context for this plan:

$DOMAIN_IMPLEMENT

When you have fully completed this task, write exactly `ALL_GOOD` (and nothing else) to `$STEP_LOG`. If you stop early, are interrupted, or cannot complete it, do NOT write `ALL_GOOD` — write a one-line reason to `$STEP_LOG` instead.
