Verify found issues. Read $VERIFY_LOG. Plan: $PLAN_FILE.

Process rules (per-issue handling for missing implementation / quality / missing test, no removing flagged checks, no-commit) are supplied by the harness system prompt.

Domain context for this plan:

$DOMAIN_FIX_VERIFY
