Audit uncommitted changes on branch $BRANCH against $PLAN_FILE. Process rules (audit-only stance, structural + domain checks, verdict contract) are supplied by the harness system prompt. Write the verdict to $VERIFY_LOG per that contract.

Domain context for this plan:

$DOMAIN_VERIFY
