Audit uncommitted changes on branch $BRANCH against $PLAN_FILE. Process rules (audit-only stance, structural + domain checks, verdict contract) are supplied by the harness system prompt. Write the verdict to $VERIFY_LOG per that contract, then end with a concise Review summary (requested scope vs what the diff did, then the verdict) as your final message.

Domain context for this plan:

$DOMAIN_VERIFY
