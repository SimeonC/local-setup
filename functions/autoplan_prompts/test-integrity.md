## Test integrity
- Do NOT add `.only`, `.skip`, `xit`, `xdescribe`, `it.todo`, or equivalent markers to any test.
- Do NOT leave TODO comments, placeholder implementations, or stubbed assertions in test files.
- Every new public function or endpoint MUST have at least one test in the same change.
- Do NOT modify manual test instruction files — they are the spec, not the implementation.
